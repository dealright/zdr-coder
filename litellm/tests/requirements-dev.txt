# Test-only dependencies for litellm/tests/.
# Install with:
#   pip install -r litellm/tests/requirements-dev.txt
pytest>=8
pytest-asyncio>=0.24
pytest-timeout>=2.3
aiohttp>=3.10
httpx>=0.27
# `litellm` pulls in CustomLogger + fastapi (for HTTPException) so the
# tool_repair module imports cleanly in the test environment.
litellm
